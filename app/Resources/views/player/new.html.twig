{% extends 'base.html.twig' %}

{% block body %}
    {% include 'admin/navbar.html.twig' %}
    <div class="container">
        <div class="card ">
            <div class="card-header">
                Editer un match
            </div>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Add" role="button" class="btn btn-success" />
            {{ form_end(form) }}



        </div></div>
{% endblock %}